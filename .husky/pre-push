branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$branch" = "main" ]; then
  echo "❌ Push directo a main prohibido. Usa PR."
  exit 1
fi

if [ "$branch" = "develop" ]; then
  echo "❌ Push directo a develop prohibido. Usa una feature branch."
  exit 1
fi

echo "✅ Branch válida: $branch"