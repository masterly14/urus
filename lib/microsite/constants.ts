/** SLA de validación comercial (M6): 2 horas desde la creación de la selección. */
export const MICROSITE_VALIDATION_SLA_MS = 2 * 60 * 60 * 1000;

/** Número preferido de propiedades (con imágenes) para un microsite de calidad. */
export const MIN_PREFERRED_PROPERTIES = 2;
