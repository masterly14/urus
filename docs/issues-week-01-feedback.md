# Issues propuestas — Feedback Demo Week 1

Estas issues quedan listas para creacion en el tracker cuando `gh` este disponible.

## P0

1. `feat(M4): implementar endpoint POST /api/whatsapp/send`
   - Criterio: envio de mensaje de prueba exitoso con proveedor seleccionado.
2. `feat(M4): implementar webhook POST /api/whatsapp/webhook`
   - Criterio: recepcion validada y emision de `WHATSAPP_RECIBIDO`.
3. `feat(M3): definir baseline LangGraph para routing de leads`
   - Criterio: grafo minimo ejecutable con persistencia de estado.

## P1

1. `test(M4): cobertura unitaria e integracion para envio y webhook`
2. `refactor(M1): observabilidad de workers (metrica por ciclo e idempotencia visible)`
3. `docs(M4): documentar contratos de plantillas WhatsApp y variables de entorno`

## P2

1. `chore(ci): instalar y autenticar gh CLI para automatizar PR/issues en Demo Day`
2. `docs(process): checklist operativo de sabado (pre-demo, post-demo, release)`
